import 'package:flutter/material.dart';
import 'package:promogames_engine/engine.dart';

Widget buildCarromPlayer(GameConfig config, GameFinished onFinished) {
  return const Scaffold(body: Center(child: Text('Carrom coming soon')));
}
